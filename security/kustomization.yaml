apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Self-reference for App of Apps pattern
  - application.yaml

  # Security apps (vault, cert-manager moved to bootstrap/)
  # external-secrets removed - using VSO (Vault Secrets Operator) instead
  - authelia/argocd.yaml
  - falco/argocd.yaml
  - kyverno/argocd.yaml
  - trivy/argocd.yaml
