apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Self-reference for App of Apps pattern
  - application.yaml

  # Security apps (vault moved to bootstrap/)
  - cert-manager/argocd.yaml
  - authelia/argocd.yaml
  - external-secrets/argocd.yaml
  - falco/argocd.yaml
