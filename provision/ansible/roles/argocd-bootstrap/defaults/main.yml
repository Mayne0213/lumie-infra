---
# ArgoCD Bootstrap Configuration

# ArgoCD Helm Chart
argocd_chart_repo: https://argoproj.github.io/argo-helm
argocd_chart_name: argo-cd
argocd_chart_version: "7.7.10"
argocd_namespace: argocd

# Git repository for app-of-apps
gitops_repo: https://github.com/Mayne0213/lumie-infra.git
gitops_branch: main

# Root app-of-apps paths (relative to repo root)
# bootstrap MUST be first - it deploys minio, vault, argocd with sync waves
app_of_apps_paths:
  - bootstrap/application.yaml
  - platform/application.yaml
  - storage/application.yaml
  - security/application.yaml
  - observability/application.yaml
  - applications/application.yaml
  - web-apps/application.yaml

# Kubeconfig path
k3s_kubeconfig: /etc/rancher/k3s/k3s.yaml

# Static secrets for bootstrap (MinIO must start before Vault)
# These MUST be provided via:
#   - ansible-vault encrypted group_vars (recommended)
#   - Extra vars: -e "minio_root_user=xxx minio_root_password=xxx"
#   - Environment: MINIO_ROOT_USER, MINIO_ROOT_PASSWORD
# minio_root_user: "{{ lookup('env', 'MINIO_ROOT_USER') }}"
# minio_root_password: "{{ lookup('env', 'MINIO_ROOT_PASSWORD') }}"
