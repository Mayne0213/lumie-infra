# Bootstrap secrets for MinIO and Vault
# Copy this file to secrets.yml and fill in the values
# Option 1: Plain file (add secrets.yml to .gitignore)
# Option 2: Encrypt with ansible-vault: ansible-vault encrypt secrets.yml

minio_root_user: "your-minio-user"
minio_root_password: "your-minio-password"
