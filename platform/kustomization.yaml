apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Platform self-reference
  - application.yaml

  # Cluster-wide resources
  - priority-classes/argocd.yaml

  # Core infrastructure
  - traefik-config/argocd.yaml
  - haproxy/argocd.yaml
  - cert-manager/argocd.yaml

  # Git server (파일 유지, Forgejo로 마이그레이션 후 삭제 예정)
  # - gitea/argocd.yaml
