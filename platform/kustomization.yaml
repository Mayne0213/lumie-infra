apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Platform self-reference
  - application.yaml

  # Cluster-wide resources
  - priority-classes/argocd.yaml

  # Core infrastructure
  - kong/argocd.yaml
  - teleport/argocd.yaml
  - coredns-config/argocd.yaml
  - cert-manager/argocd.yaml

  # GitHub redirect
  - gitea/argocd-redirect.yaml

  # LUMIE Event Bus
  - rabbitmq-operator/argocd.yaml
  - rabbitmq/argocd.yaml
