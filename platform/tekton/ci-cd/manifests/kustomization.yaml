apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Secrets
  - secrets/gitea-credentials.yaml
  # Tasks
  - tasks/kaniko-clone-build.yaml
  # Pipelines
  - pipelines/nextjs-pipeline.yaml
  - pipelines/fastapi-pipeline.yaml
  - pipelines/python-pipeline.yaml
  - pipelines/react-pipeline.yaml
  - pipelines/springboot-pipeline.yaml
  # Triggers
  - triggers/
  # Cleanup CronJob
  - cleanup/cronjob.yaml
