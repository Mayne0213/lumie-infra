{{- if .Values.middleware }}
{{- if .Values.middleware.enabled }}
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ .Values.middleware.name | default (printf "%s-middleware" (include "web-app.fullname" .)) }}
  labels:
    {{- include "web-app.labels" . | nindent 4 }}
spec:
  {{- /* Traefik middleware can only have ONE type - check forwardAuth first, then stripPrefix */}}
  {{- if and .Values.middleware.forwardAuth .Values.middleware.forwardAuth.address }}
  forwardAuth:
    address: {{ .Values.middleware.forwardAuth.address }}
    {{- if .Values.middleware.forwardAuth.trustForwardHeader }}
    trustForwardHeader: {{ .Values.middleware.forwardAuth.trustForwardHeader }}
    {{- end }}
    {{- if .Values.middleware.forwardAuth.authResponseHeaders }}
    authResponseHeaders:
      {{- range .Values.middleware.forwardAuth.authResponseHeaders }}
      - {{ . }}
      {{- end }}
    {{- end }}
  {{- else if and .Values.middleware.stripPrefix .Values.middleware.stripPrefix.prefixes }}
  stripPrefix:
    prefixes:
      {{- range .Values.middleware.stripPrefix.prefixes }}
      - {{ . }}
      {{- end }}
  {{- end }}
{{- end }}
{{- end }}
