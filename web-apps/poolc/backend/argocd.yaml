apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: poolc-backend
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd-image-updater.argoproj.io/image-list: poolc-backend=zot0213.kro.kr/web-apps/poolc-backend:latest
    argocd-image-updater.argoproj.io/poolc-backend.update-strategy: digest
    argocd-image-updater.argoproj.io/poolc-backend.platforms: linux/arm64/v8
    argocd-image-updater.argoproj.io/poolc-backend.pull-secret: pullsecret:argocd/zot-registry-credentials
    argocd-image-updater.argoproj.io/write-back-method: git:secret:argocd/gitea-creds
    argocd-image-updater.argoproj.io/git-branch: main
    argocd-image-updater.argoproj.io/poolc-backend.helm.image-tag: image.tag
    argocd-image-updater.argoproj.io/git-commit-message: "CHORE(app): update {{ .AppName }} image"
spec:
  project: default
  sources:
  - repoURL: https://github0213.com/Mayne0213/K3S-HOME.git
    targetRevision: main
    path: web-apps/charts/web-app
    helm:
      valueFiles:
      - $values/web-apps/poolc/backend/helm-values.yaml
  - repoURL: https://github0213.com/Mayne0213/K3S-HOME.git
    targetRevision: main
    ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: poolc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
    - CreateNamespace=true
    - PrunePropagationPolicy=foreground
    - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    managedNamespaceMetadata:
      labels:
        goldilocks.fairwinds.com/enabled: 'true'
        zot-registry: 'enabled'
  revisionHistoryLimit: 10
