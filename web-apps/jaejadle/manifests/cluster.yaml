apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: jaejadle-db
  namespace: jaejadle
spec:
  instances: 2
  imageName: zot0213.kro.kr/storage/postgresql:16.6
  bootstrap:
    initdb:
      database: JaejadleDB
      owner: jaejadle
  storage:
    storageClass: local-path
    size: 1Gi
  resources:
    requests:
      memory: "512Mi"
      cpu: "50m"
    limits:
      memory: "512Mi"
  monitoring:
    enablePodMonitor: true
  affinity:
    podAntiAffinityType: required
  backup:
    barmanObjectStore:
      destinationPath: s3://jaejadle
      endpointURL: https://s3.minio0213.kro.kr
      s3Credentials:
        accessKeyId:
          name: jaejadle-db-backup-creds
          key: ACCESS_KEY_ID
        secretAccessKey:
          name: jaejadle-db-backup-creds
          key: ACCESS_SECRET_KEY
      wal:
        compression: gzip
      data:
        compression: gzip
    retentionPolicy: "7d"
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: jaejadle-db-backup
  namespace: jaejadle
spec:
  schedule: "0 19 * * *"
  backupOwnerReference: self
  cluster:
    name: jaejadle-db
