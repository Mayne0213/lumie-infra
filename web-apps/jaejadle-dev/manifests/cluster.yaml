apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: jaejadle-dev-db
  namespace: jaejadle-dev
spec:
  instances: 1
  imageName: zot0213.kro.kr/storage/postgresql:16.6
  bootstrap:
    initdb:
      database: JaejadleDevDB
      owner: jaejadle-dev
  storage:
    storageClass: local-path-retain
    size: 5Gi
  resources:
    requests:
      memory: "256Mi"
      cpu: "50m"
    limits:
      memory: "512Mi"
  monitoring:
    enablePodMonitor: true

  backup:
    barmanObjectStore:
      destinationPath: s3://cnpg-backups/jaejadle-dev/jaejadle-dev-db
      endpointURL: http://minio.minio.svc.cluster.local:9000
      s3Credentials:
        accessKeyId:
          name: minio-s3-credentials
          key: MINIO_ACCESS_KEY_ID
        secretAccessKey:
          name: minio-s3-credentials
          key: MINIO_SECRET_ACCESS_KEY
    retentionPolicy: "7d"
---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: jaejadle-dev-db-backup
  namespace: jaejadle-dev
spec:
  schedule: "20 4 * * *"
  backupOwnerReference: self
  cluster:
    name: jaejadle-dev-db
