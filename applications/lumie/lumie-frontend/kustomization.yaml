apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmGlobals:
  chartHome: ../../../charts

helmCharts:
  - name: common
    releaseName: lumie-frontend
    namespace: lumie-frontend
    valuesFile: common-values.yaml

resources:
  - manifests/ingress.yaml
