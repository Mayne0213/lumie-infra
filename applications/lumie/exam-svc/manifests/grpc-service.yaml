apiVersion: v1
kind: Service
metadata:
  name: exam-svc-grpc
  namespace: lumie-exam
  labels:
    app: exam-svc
    service-type: grpc
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: 9090
      targetPort: 9090
      protocol: TCP
  selector:
    app: exam-svc
