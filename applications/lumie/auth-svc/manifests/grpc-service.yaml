apiVersion: v1
kind: Service
metadata:
  name: auth-svc-grpc
  namespace: lumie-platform
  labels:
    app: auth-svc
spec:
  type: ClusterIP
  ports:
  - name: grpc
    port: 9090
    targetPort: 9090
    protocol: TCP
  selector:
    app: auth-svc
