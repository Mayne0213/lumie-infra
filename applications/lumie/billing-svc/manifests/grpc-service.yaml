apiVersion: v1
kind: Service
metadata:
  name: billing-svc-grpc
  namespace: lumie-platform
  labels:
    app: billing-svc
spec:
  type: ClusterIP
  ports:
  - name: grpc
    port: 9090
    targetPort: 9090
    protocol: TCP
  selector:
    app: billing-svc
