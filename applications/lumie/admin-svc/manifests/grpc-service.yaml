apiVersion: v1
kind: Service
metadata:
  name: admin-svc-grpc
  namespace: lumie-admin
  labels:
    app: admin-svc
spec:
  type: ClusterIP
  ports:
  - name: grpc
    port: 9090
    targetPort: 9090
    protocol: TCP
  selector:
    app: admin-svc
