apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmGlobals:
  chartHome: ../../../charts

helmCharts:
  - name: common
    releaseName: spreadsheet-svc
    namespace: lumie-spreadsheet
    valuesFile: common-values.yaml

resources:
  - manifests/grpc-service.yaml
  - manifests/websocket-ingress.yaml
