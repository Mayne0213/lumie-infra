apiVersion: v1
kind: Service
metadata:
  name: tenant-svc-grpc
  namespace: lumie-platform
  labels:
    app: tenant-svc
spec:
  type: ClusterIP
  ports:
  - name: grpc
    port: 9090
    targetPort: 9090
    protocol: TCP
  selector:
    app: tenant-svc
