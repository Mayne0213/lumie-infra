apiVersion: v1
kind: Service
metadata:
  name: tenant-svc
  namespace: lumie-platform
  labels:
    app: tenant-svc
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app: tenant-svc
