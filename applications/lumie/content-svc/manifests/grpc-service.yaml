apiVersion: v1
kind: Service
metadata:
  name: content-svc-grpc
  namespace: lumie-content
  labels:
    app: content-svc
    service-type: grpc
spec:
  type: ClusterIP
  ports:
    - name: grpc
      port: 9090
      targetPort: 9090
      protocol: TCP
  selector:
    app: content-svc
