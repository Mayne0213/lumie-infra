apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmGlobals:
  chartHome: ../../../charts

helmCharts:
  - name: common
    releaseName: content-svc
    namespace: lumie-content
    valuesFile: common-values.yaml

resources:
  - manifests/grpc-service.yaml
