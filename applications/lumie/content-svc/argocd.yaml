apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: content-svc
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/Mayne0213/lumie-infra.git
    targetRevision: main
    path: applications/lumie/content-svc
  destination:
    server: https://kubernetes.default.svc
    namespace: lumie-content
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    syncOptions:
    - CreateNamespace=true
  revisionHistoryLimit: 10
