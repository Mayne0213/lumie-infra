apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lumie-document
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  # Note: Image updates are handled by Tekton pipeline (git-update-values task)
spec:
  project: default
  source:
    repoURL: https://github.com/Mayne0213/lumie-infra.git
    targetRevision: main
    path: applications/lumie-document
  destination:
    server: https://kubernetes.default.svc
    namespace: lumie-document
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
    - CreateNamespace=true
    - PrunePropagationPolicy=foreground
    - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
    managedNamespaceMetadata:
      labels:
        goldilocks.fairwinds.com/enabled: 'true'
  revisionHistoryLimit: 10
