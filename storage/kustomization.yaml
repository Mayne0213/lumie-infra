apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Self-reference for App of Apps pattern
  - application.yaml

  # Storage systems (minio, zot moved to bootstrap/)
  # Database tools
  - pgweb/argocd.yaml
  # Operators
  - cnpg/argocd.yaml
  - velero/argocd.yaml
