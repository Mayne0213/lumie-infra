apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Self-reference for App of Apps pattern
  - application.yaml

  # Database tools
  - pgweb/argocd.yaml
  # Operators
  - cnpg/argocd.yaml
  - velero/argocd.yaml

  # LUMIE Cache
  - redis/argocd.yaml

  # Connection pooling for multi-tenancy
  - pgbouncer/argocd.yaml

  # Shared infrastructure database (Teleport, etc.)
  - infra-db/argocd.yaml
